# 合成后处理是独立工作流阶段

工作流在 `composite` 与 `export` 之间增加权威的 `post_process` 阶段：`composite` 负责抠像修边与线性光 Alpha 合成并发布 16-bit 无损基础帧，`post_process` 负责执行有序效果链并物化全分辨率 16-bit 无损处理后帧，`export` 只负责编码。该边界使修边、效果链和导出配置分别从正确的阶段开始失效；空效果链仍发布一个经过验证的无效果后处理结果，使导出始终只有一个权威输入契约。缓存处理后帧会增加存储占用，但修改编码设置、重试或取消导出都不需要重新执行效果链。
