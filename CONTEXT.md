# GS Video

GS Video 将源视频中已经分割好的前景像素，与从同一完整视频解算出的相机运动一起迁移到 Gaussian Splatting 场景。前景始终是屏幕空间 RGBA 层，不是位于场景中的三维人物模型。

## Language

**完整视频解算（Raw-video Camera Solve）**：
ViPE 直接读取完整 RGB 帧并联合输出逐帧内参、camera-to-world 位姿和深度。人物 Mask 不修改 ViPE 输入，只在后续源地面拟合时排除前景像素。
_Avoid_: 去人物后解算、Mask 后解算、固定 60° FOV、消失点校准

**源像素坐标系（Observed Source Raster）**：
源视频现有宽高定义的像素坐标系，也是 ViPE 内参 `K(t)` 的坐标系。它描述已观察到的画面，不声明原始传感器尺寸或裁剪前画布。
_Avoid_: 传感器画布、原始完整画幅

**源地面（Source Ground）**：
由 ViPE 深度在锚帧中自动拟合并通过几何审计的平面。拟合时只排除锚帧前景 Mask；不使用脚底像素，也不把人物可见性当作接触证据。
_Avoid_: 脚底平面、人物接触面、手工地平线

**源地面原点（Source Ground Origin）**：
锚帧相机中心向源地面作正交投影得到的点，是源轨迹映射到目标 GS 的原点。
_Avoid_: 人物脚点、源人物位置

**探索相机（Exploration Camera）**：
只用于自由浏览 GS、提交三个近似地面提示，并为自动地面候选提供可见性证据的完整 6DoF 相机；不直接成为最终合成相机。
_Avoid_: 初始机位、目标相机、冻结相机

**地面提示（Ground Hints）**：
用户在探索视口中点击的三个近似位置。系统结合每个点击邻域的 GS 中心、深度连续性、Opacity 和连通支持自动拟合地面；点击本身不作为最终三维点 authority。
_Avoid_: 精确三点、逐点确认

**目标地面（Target Ground）**：
通过三个提示邻域共同支持并经几何审计的 GS 平面。系统将点击射线与拟合平面求交得到精化后的 P0/P1/P2，并让法线自动朝向探索相机。
_Avoid_: 手工翻转法线、点击三角形

**目标原点 P0（Target Origin P0）**：
源地面原点在目标 GS 中的对应点。它定位整条相机轨迹，不表示人物脚底或场景世界原点。
_Avoid_: 人物站位、接触点、Orbit Pivot

**GS 比例（GS Scale）**：
将 ViPE 世界中的相机高度和位移统一映射到 GS 单位的唯一尺度参数。它不缩放前景 RGBA，也不与另一项运动倍率相乘。
_Avoid_: 人物与场景比例、motion scale、人物缩放

**场景方位角（Scene Azimuth）**：
源轨迹地面基底映射到目标地面后，绕目标地面法线的创作旋转。它不是探索相机的局部 Yaw。
_Avoid_: 相机局部 Yaw、人物旋转

**屏幕空间前景（Screen-space Foreground）**：
源视频分割得到的逐帧 RGBA 像素层。它与对应帧的相机内参和像素坐标保持固定关系，只随最终画布和裁剪整体变换，绝不在三维空间中独立平移、旋转或缩放。
_Avoid_: 人物 Plane、人物模型、定位人物

**虚拟相机画布（Virtual Camera Canvas）**：
以源像素坐标为基准、但不受源宽高边界限制的概念画布。实现不需要先物化一张无限或超大图；渲染器可直接为固定输出裁剪平移 ViPE 主点，源 RGBA 仅在与源像素相交的子矩形内存在，其他区域前景 Alpha 恒为零。
_Avoid_: 恢复原传感器画幅、源宽高比约束

**固定输出裁剪（Fixed Output Crop）**：
在源像素坐标中定义、对全片保持不变的输出矩形；X/Y 可以为负，矩形可以部分或完全超出源画面。系统直接为该矩形渲染 GS 背景并放置前景像素。修改它使 GS 渲染、合成和导出失效，但不重新运行 ViPE 或轨迹映射。
_Avoid_: 源画面内裁剪、逐帧裁剪、重新解算 FOV
